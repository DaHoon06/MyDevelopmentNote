export const ENV = {
    ClientID: process.env.ClientID as string,
    SCOPE: process.env.SCOPE as string,
    MongoDB_URI: process.env.Mongoose_URI as string,
    KEY_JWT: process.env.JWT_KEY as string
}